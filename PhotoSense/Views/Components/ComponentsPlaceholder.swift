import SwiftUI

// TODO: Add reusable UI components for PhotoSense (e.g. stat cards, buttons, chips)
struct ComponentsPlaceholder: View {
    var body: some View {
        Text("Components Placeholder")
    }
}

#Preview {
    ComponentsPlaceholder()
}
